<div class='jumbotron'>
  {{#em-form model=model submit_button=false}}
    {{em-input property="password" label="Password" type="password" placeholder="Enter your current password"}}
  {{/em-form}}

  {{em-button on-click='submit' default='Cancel' class='btn btn-danger' executing='Canceling' resolved='Canceled' rejected="Failed"}}
</div>
