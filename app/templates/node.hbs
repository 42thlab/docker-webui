<div class='jumbotron'>
  <div>id: {{model.id}}</div>
  <div>name: {{model.name}}</div>

  {{#link-to 'node.edit'}}<button class='btn btn-warning'>Edit</button>{{/link-to}}
  <button {{action 'delete'}} class='btn btn-danger'>Delete</button>
</div>
